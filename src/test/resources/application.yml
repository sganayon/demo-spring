server:
  port: 8080

spring:
  jpa:
    hibernate.ddl-auto: none
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
  dataSource:
    url: jdbc:h2:file:./.h2/test/database;
    driver-class-name: org.h2.Driver

h2:
  console:
    enabled: true
    path: /h2-console


